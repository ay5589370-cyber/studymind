{
  "name": "studymind-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node -e \"console.log('No build step required')\"",
    "start": "node local-server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
